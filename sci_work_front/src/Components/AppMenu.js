import React from 'react';
import '../css/AppMenu.css';

const AppMenu = () => {
    return (
        <nav>
            <ul className="menu">
            </ul>
        </nav>
    )}

export default AppMenu