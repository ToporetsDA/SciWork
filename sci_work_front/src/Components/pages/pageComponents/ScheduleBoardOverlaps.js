import React, { useEffect, useState, useCallback, useMemo } from 'react';
import '../../../css/pages/pageComponents/ScheduleBoard.css';

const ScheduleBoardOverlaps = ({ data, state, setState, currentScale, setCurrentScale, gridValues, setGridValues, intervalAnchor, scheduleBoard }) => {

    

    return (
        <></>
    )}

export default ScheduleBoardOverlaps